# E-commerce project

Проект для домашнего задания по ООП.

## Описание

В проекте реализованы базовые сущности интернет-магазина:

- `Product` — товар;
- `Category` — категория товаров.

## Класс `Product`

Класс `Product` описывает товар.

Атрибуты:

- `name` — название товара;
- `description` — описание товара;
- `price` — цена товара;
- `quantity` — количество товара в наличии.

Пример:

```python
from src.classes import Product

product = Product(
    name="Samsung Galaxy S23 Ultra",
    description="256GB, Серый цвет, 200MP камера",
    price=180000.0,
    quantity=5,
)

## Домашнее задание 14.2

В проект добавлена защита данных через приватные атрибуты, геттеры, сеттеры и класс-методы.

### Изменения в классе `Category`

- список товаров сделан приватным атрибутом `__products`;
- добавлен метод `add_product`;
- добавлен геттер `products` через декоратор `@property`;
- геттер возвращает список товаров в виде строки.

Формат строки товара:

```text
Название продукта, X руб. Остаток: X шт.

## Домашнее задание 15.1

В проект добавлены магические методы для строкового представления объектов и сложения товаров.

### Изменения в классе `Product`

Добавлен метод `__str__`, который возвращает строку в формате:

```text
Название продукта, X руб. Остаток: X шт.
```

Добавлен метод `__add__`, который возвращает общую стоимость двух товаров на складе:

```text
цена первого товара × количество первого товара + цена второго товара × количество второго товара
```

### Изменения в классе `Category`

Добавлен метод `__str__`, который возвращает строку в формате:

```text
Название категории, количество продуктов: X шт.
```

Количество продуктов считается как сумма значений `quantity` всех товаров в приватном списке товаров категории.

Геттер `products` оптимизирован: теперь он использует строковое представление каждого объекта `Product`.

### Тестирование

Добавлены тесты для:

- строкового представления товара;
- строкового представления категории;
- сложения товаров через магический метод `__add__`.

